import { AttrDirDirective } from './attr-dir.directive';

describe('AttrDirDirective', () => {
  it('should create an instance', () => {
    const directive = new AttrDirDirective();
    expect(directive).toBeTruthy();
  });
});
