import { TogglePipePipe } from './toggle-pipe.pipe';

describe('TogglePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TogglePipePipe();
    expect(pipe).toBeTruthy();
  });
});
